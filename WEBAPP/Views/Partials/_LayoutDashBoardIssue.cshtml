@*@using UtilityLib
@using WEBAPP.Helper
@using FluentBootstrap
@model WEBAPP.Areas.Users.Models.LoginViewModel
@using (var form = Html.Bootstrap().Form().SetId("form1").SetHorizontal().Begin())
{    @form.ValidationSummary(true).AddCss(Css.Hidden)
    @Html.AntiForgeryToken()
    using (var row1 = Html.Bootstrap().GridRow().Begin())
    {
        using (row1.GridColumn(4).Begin())
        {
            @Html.GetVSMInfoBoxFor(t => t.Username, "#0000ff")
        }

        using (row1.GridColumn(4).Begin())
        {
            @Html.GetVSMInfoBoxFor(t => t.Username, "#009900")
        }

        using (row1.GridColumn(4).Begin())
        {
            @Html.GetVSMInfoBoxFor(t => t.Username, "#ff8000")
        }
    }
}*@